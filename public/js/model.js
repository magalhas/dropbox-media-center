define(function (require) {
  "use strict";
  /**
   * @class module:app~App.Model
   * @extends external:Backbone.Model
   */
  return Backbone.Model.extend(
  /** @lends module:app~App.Model.prototype */
  {
  });
});